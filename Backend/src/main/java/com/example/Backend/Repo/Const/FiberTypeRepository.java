package com.example.Backend.Repo.Const;

import com.example.Backend.Model.Const.FiberType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FiberTypeRepository extends JpaRepository<FiberType, Long> {
}
